.container_match
  .title_match
    = t('.title')

  = form_for @match do |f|
    = errors_for(@match).html_safe
    
    = render  "select_teams", :f => f
    .span2.offset0.2
      %div
        %button.btn.btn-info
          .btn-block.pull-rigth{ :href => f.submit }= t('.btn_create')